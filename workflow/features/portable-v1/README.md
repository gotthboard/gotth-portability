# Portable v1

This completed feature replaces the placeholder with an admitted, unreleased,
consumer-neutral streaming portability mechanism. State and required gates live
in `workflow.toml`; evidence here remains supporting material. Release and
compatibility remain gated by a real downstream consumer schema, behavioral
verification, and exact dependency pin.
